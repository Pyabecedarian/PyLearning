"""
Standard sequence operations(eg. indexing, slicing, multiplications,
membership, len(), min(), max() ) work with strings.

BUT, as strings are immutable, all kinds of item or slice assignments
are ILLEGAL.
"""

